
TODO:
.htaccess ? interdire le listing -> check lien
ajouter alerte mauvais login


favicon p3

TOASK:

htmlspecialchars bien inutile en entrée ?
isPortrait: function(){
$(function(){
  if($(window).height() > $(window).width()){
    console.log('portrait');
    return true;
  }
  return false;
})
